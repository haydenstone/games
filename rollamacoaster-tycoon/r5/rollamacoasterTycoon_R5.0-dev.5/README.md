# R5.0-dev.5
Ava is now the persistent observer/learner entity. On each runtime startup or Reset iteration, Ava selects one random addressable verse from the externally mounted Codex and reflects on that smallest JSON unit. Her bounded structured memory survives Reset and records observations. Her memory is not authoritative world truth.

Codex is external and is NOT packaged. Default host path: `$HOME/Documents/codex.json`.

Use `./start.sh status|start|stop|restart|build|test|logs|info`.
