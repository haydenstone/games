# Rollamacoaster Tycoon R5.0-dev.7

Primitive-retirement proof: world-runtime-v3 makes simulation time authoritative and persistent. Process startup never wakes a stopped simulation. Ava's perception, wisdom, happiness, timeline, and simulated memory formation advance only on simulation ticks.

The Codex is external at `$HOME/Documents/codex.json`. It is not packaged in releases.

## Minimum runtime

```bash
./start.sh start
```

Open http://127.0.0.1:8765. Use `./start.sh stop`, `./start.sh restart`, `./start.sh status`, and `./start.sh test` as needed.
