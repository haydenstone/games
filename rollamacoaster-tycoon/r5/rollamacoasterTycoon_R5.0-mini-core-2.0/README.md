# R5 Mini Core 2.0

Production rebake of the Mini Core 1.1 Charlie behavioral contract.

Mini Core 2.0 keeps one authoritative World Kernel, the 50 Hz deterministic simulation clock, smooth presentation projections, eight UUID-bearing XYZ/presence containers, Kernel Lineage Explorer, kernel-first intents, external read-only Codex, persistent simulation state, and one public gameplay port.

Codex cognition now selects one complete chapter as the authoritative read-only context for a reflection session. Eligible personified actors share that chapter source while retaining independent actor-owned perception, interpersonalization, reflection, and history. Llama is asked for a more contemplative 140–220 word integrated reflection rather than a short sampler response.

The production normalization begins by extracting Codex indexing/context mechanics behind `src/core/codex.mjs` instead of keeping parsing policy embedded in the HTTP runtime. The Kernel remains the sole authority.
