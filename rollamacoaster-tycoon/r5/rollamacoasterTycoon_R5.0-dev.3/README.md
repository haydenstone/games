# rollamacoasterTycoon R5.0-dev.3

Architectural proof: one authoritative entity travels a timeline while a deterministic simulation runs. The simulation can start/stop. Codex availability is a relationship/state condition. When available, the entity actively perceives the smallest included verse unit (Psalm 119:24) and derives a small wisdom observation from it. Happiness and wisdom evolve from rules rather than direct UI mutation.

The project remains one Docker container and one exposed gameplay port: 8765.
